export enum SortOptions {
  CreatedAt = "Created",
  Name = "Name",
  Description = "Description",
  Weight = "Weight",
  Packed = "Packed",
}

export enum FilterOptions {
  NotInList = "Not in current list",
}
